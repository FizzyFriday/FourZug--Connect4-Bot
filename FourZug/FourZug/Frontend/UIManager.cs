using FourZug.Frontend.Forms;

namespace FourZugBot.Frontend
{
    // Handles UI changes (Originally done in GameFlow.cs / GameManager.cs)
    internal static class UIManager
    {
        // Displays game onto screen
        public static void DisplayBoard()
        {
            /*
            // Replace with UI Manager methods
            // Running down the grid
            for (int r = 5; r >= 0; r--)
            {
                // Display top header
                Console.WriteLine(new string('-', 43));
                string col = "";
                // Display row contents
                for (int c = 0; c < 7; c++)
                {
                    col += $"|  {grid[c, r]}  ";
                }
                col += "|";
                Console.WriteLine(col);
            }
            //Display bottom of grid
            Console.WriteLine(new string('-', 43));
            */
        }

        public static void DisplayPlayerTurn()
        { 
        
        }
    }
}
